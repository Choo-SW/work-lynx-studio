# TCCINS 워크플로우 빌더 사용 가이드

## 📋 전자결재 프로세스 정의

### 노드 타입 (9종)

#### 1. **기안 (Start)**
- 문서 작성 및 결재 시작
- 결재 프로세스의 시작점

#### 2. **검토 (Review)**
- 담당자가 문서를 검토합니다
- **반려 가능**: 기본적으로 반려 가능 (설정 변경 가능)
- **N번 반복**: 여러 검토자 순차 지정 가능
- 설정 옵션:
  - 반려 가능 여부
  - 필수 여부
  - 병렬 모드 (동시 검토)

#### 3. **합의 (Agreement)**
- 타부서 또는 관련 부서의 의견 수렴
- **반려 가능 여부**: 옵션 처리 (기본값: 불가능)
- **N번 반복**: 여러 합의자 순차 지정 가능
- 설정 옵션:
  - 반려 가능 여부 선택
  - 필수 여부
  - 병렬 모드 (동시 합의)

#### 4. **승인 (Approval)**
- 최종 승인 단계
- **문서번호 채번**: 승인 시 자동으로 문서번호 생성
- **내용 변경 불가**: 승인 후 문서 수정 불가능
- 설정 옵션:
  - 문서번호 채번 여부
  - 내용 잠금 여부
  - 문서번호 접두사 (예: APPR-2024-)

#### 5. **시행 (Execution)**
- 외부 기관에 문서 발송
- **문서번호 채번**: 시행 시 문서번호 생성
- **내용 변경 불가**: 시행 후 문서 수정 불가능
- 설정 옵션:
  - 문서번호 채번 여부
  - 내용 잠금 여부
  - 문서번호 접두사 (예: DOC-2024-)

#### 6. **접수 (Receipt)**
- 외부 기관의 회신 문서 접수
- **접수번호 채번**: 접수 시 자동으로 접수번호 생성
- **내부 승인 재시작**: 접수 후 내부 결재 프로세스 재개
- 설정 옵션:
  - 접수번호 채번 여부
  - 수신 부서 지정
  - 내부 승인 재시작 여부

#### 7. **반려 (Reject)**
- 문서를 기안자에게 반송
- 재작성 후 다시 제출 가능

#### 8. **조건 분기 (Condition)**
- 조건에 따라 다른 경로로 분기
- 예시:
  - 금액 > 500만원: 대표이사 승인
  - 금액 ≤ 500만원: 부서장 승인
  - 부서 = 재무팀: 법무팀 합의 추가

#### 9. **종료 (End)**
- 결재 프로세스 완료

---

## 🚀 빠른 시작

### 방법 1: 템플릿으로 시작 (추천)

1. 홈페이지에서 "Workflow Builder" → "템플릿으로 시작하기" 클릭
2. 3가지 템플릿 중 선택:
   - **기본 결재**: 기안 → 검토 → 승인
   - **합의 포함**: 기안 → 검토 → 합의 → 승인
   - **외부 발송**: 기안 → 검토 → 합의 → 시행 → 접수 → 기안 → 검토 → 승인
3. 템플릿이 로드되면 노드를 클릭하여 담당자 정보 수정
4. 저장 버튼으로 수정된 워크플로우 다운로드

### 방법 2: 빈 워크플로우로 시작

1. 홈페이지에서 "Workflow Builder" → "빈 워크플로우로 시작하기" 클릭
2. 노드 팔레트에서 필요한 노드를 클릭하여 추가
3. 노드를 드래그하여 원하는 위치에 배치
4. 노드 간 연결: 노드 하단의 핸들을 다음 노드 상단으로 드래그
5. 노드를 클릭하여 Drawer에서 상세 설정

---

## 🎯 노드 설정 방법

### 공통 설정
- **노드 이름**: 노드의 표시 이름 (예: "부서장 검토", "대표이사 승인")
- **설명**: 노드에 대한 상세 설명

### 담당자 정보 (검토/합의/승인/시행/접수)
- **담당자 이름**: 실제 담당자 이름 (예: 홍길동)
- **부서**: 소속 부서 (예: 재무팀)
- **직급**: 직급 선택 (사원, 주임, 대리, 과장, 차장, 부장, 이사, 상무, 전무, 대표이사)
- **순서**: N번 반복 시 순서 지정 (1, 2, 3...)

### 검토/합의 노드 추가 설정
- **반려 가능 여부**: 
  - 검토자: 기본 "가능"
  - 합의자: 기본 "불가능" (옵션 처리)
- **필수 여부**: 해당 단계 통과 필수 여부
- **병렬 처리**: 여러 담당자가 동시에 처리 (병렬) vs 순차 처리

### 승인/시행 노드 추가 설정
- **문서번호 채번**: 승인/시행 시 자동으로 문서번호 생성
- **내용 변경 잠금**: 승인/시행 후 문서 수정 금지
- **문서번호 접두사**: 문서번호 앞에 붙는 접두사 (예: APPR-2024-, DOC-2024-)

### 접수 노드 추가 설정
- **접수번호 채번**: 접수 시 자동으로 접수번호 생성
- **수신 부서**: 문서를 수신하는 부서명
- **내부 승인 재시작**: 접수 후 내부 결재 프로세스 자동 시작

### 조건 분기 노드 설정
- **조건 필드**: 비교할 필드 선택 (금액, 부서, 직급 등)
- **연산자**: 비교 연산자 (>, ≥, <, ≤, =, ≠)
- **조건 값**: 비교 기준 값 (예: 5000000)

---

## 📝 3가지 표준 프로세스

### 1. 기본 결재 프로세스

```
기안 → 검토 → 승인 → 완료
```

**특징:**
- 검토자 반려 가능
- 승인 시 문서번호 채번
- 승인 후 내용 변경 불가

**사용 예시:** 일반 품의서, 휴가신청서, 출장신청서

---

### 2. 합의 포함 결재 프로세스

```
기안 → 검토 → 합의 → 승인 → 완료
```

**특징:**
- 검토자 반려 가능
- 합의자 반려 불가 (의견만 제시)
- 승인 시 문서번호 채번

**사용 예시:** 법무 검토가 필요한 계약서, 타부서 협조가 필요한 기안

---

### 3. 외부 발송 결재 프로세스 (복합)

```
[외부 발송]
기안 → 검토 → 합의 → 시행 (외부 발송)
         ↓
[회신 접수 후 내부 승인]
접수 → 기안 → 검토 → 승인 → 완료
```

**특징:**
- 시행 시 문서번호 채번 (외부 발송용)
- 접수 시 접수번호 채번
- 접수 후 내부 승인 재시작
- 최종 승인 시 문서번호 미채번 (접수 문서이므로)

**사용 예시:** 
- 정부 기관에 공문 발송 후 회신 접수
- 타 기업에 협조 요청 후 답변 접수
- 외부 기관과의 왕복 문서 처리

---

## 💡 실전 활용 팁

### 1. N번 반복 검토/합의 설정 방법

**시나리오:** 팀장 → 부서장 → 본부장 순차 검토

1. 검토 노드 3개 추가
2. 각 노드 설정:
   - 노드 1: 이름 "팀장 검토", 순서 1, 직급 "과장"
   - 노드 2: 이름 "부서장 검토", 순서 2, 직급 "부장"
   - 노드 3: 이름 "본부장 검토", 순서 3, 직급 "이사"
3. 노드를 순차적으로 연결

### 2. 조건 분기 활용

**시나리오:** 500만원 이상은 대표이사 승인, 미만은 부서장 승인

1. 조건 분기 노드 추가
2. 설정:
   - 조건 필드: "금액"
   - 연산자: "≥"
   - 조건 값: "5000000"
3. 두 개의 승인 노드 추가 (대표이사, 부서장)
4. 조건 분기에서 각각 연결

### 3. 병렬 합의 처리

**시나리오:** 법무팀과 재무팀 동시 합의

1. 합의 노드 2개 추가
2. 각 노드 설정:
   - 노드 1: "법무팀 합의", 병렬 모드: "병렬"
   - 노드 2: "재무팀 합의", 병렬 모드: "병렬"
3. 이전 노드에서 두 합의 노드로 각각 연결
4. 두 합의 노드를 다음 노드로 연결

### 4. 반려 처리 흐름

**권장 패턴:**
```
기안 → 검토 → 승인
  ↑      ↓
  └── 반려
```

1. 검토 노드에서 "반려 가능" 옵션 활성화
2. 반려 노드 추가
3. 검토 노드 → 반려 노드 연결
4. 반려 노드 → 기안 노드 연결 (재작성 후 재제출)

---

## 🔄 워크플로우 관리

### 저장
- "저장" 버튼 클릭
- JSON 파일로 다운로드
- 파일명: `workflow-{템플릿ID}-{타임스탬프}.json`

### 불러오기 (예정)
- "불러오기" 버튼 클릭
- 저장된 JSON 파일 선택
- 워크플로우 복원

### 배포 (예정)
- "배포" 버튼 클릭
- 실제 전자결재 시스템에 적용
- 버전 관리 및 롤백 지원

---

## 📊 노드 시각화

각 노드는 gradient 배경색과 아이콘으로 구분됩니다:

- 🟣 **기안**: 보라색, 문서 아이콘
- 🔵 **검토**: 파란색, 감사 아이콘
- 🔵 **합의**: 청록색, 사용자 추가 아이콘
- 🔴 **승인**: 핑크색, 체크 아이콘
- 🟢 **시행**: 녹색, 발송 아이콘
- 🟠 **접수**: 주황색, 받은편지함 아이콘
- 🔴 **반려**: 빨간색, X 아이콘
- 🟠 **조건 분기**: 주황색, 분기 아이콘
- ⚪ **종료**: 회색, 깃발 아이콘

노드 안에 설정된 정보가 Tag로 표시됩니다:
- 🔵 직급 Tag: 담당자 직급
- 🔴 반려 가능 Tag
- 🟠 필수 Tag
- 🔵 병렬 Tag
- 🟢 문서번호 채번 Tag
- 🔴 변경 불가 Tag
- 🟢 접수번호 채번 Tag
- 🔵 내부 승인 재시작 Tag

---

## ⚙️ 기술 스택

- **React Flow**: 워크플로우 시각화
- **Ant Design**: UI 컴포넌트
- **TypeScript**: 타입 안전성
- **Next.js 15**: App Router

---

## 🆘 문제 해결

### 노드가 추가되지 않아요
- 노드 팔레트에서 버튼을 클릭하면 자동으로 추가됩니다
- 캔버스에서 드래그하여 원하는 위치로 이동하세요

### 노드 연결이 안돼요
- 노드 하단의 작은 원(핸들)을 클릭한 상태에서
- 다음 노드 상단의 핸들로 드래그하세요

### 설정이 저장되지 않아요
- Drawer 우측 상단의 "저장" 버튼을 꼭 클릭하세요
- "취소" 버튼은 변경사항을 버립니다

### 템플릿이 로드되지 않아요
- 브라우저 콘솔에서 에러 메시지 확인
- 템플릿 JSON 파일이 `/workflows/` 폴더에 있는지 확인
- 개발 서버가 정상 실행 중인지 확인

---

## 📚 다음 단계

- [ ] 워크플로우 불러오기 기능 구현
- [ ] 조건 분기 엣지 라벨 표시
- [ ] 워크플로우 구조 검증 (시작/종료 노드 필수 체크)
- [ ] Form Builder와 워크플로우 연결
- [ ] Legacy ERP와 API 연동

---

**문의:** TCCINS 개발팀
**버전:** 1.0.0 (2024-01-11)
