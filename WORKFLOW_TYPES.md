# 워크플로우 빌더 가이드

## 📚 4가지 워크플로우 타입

### 1. 전자결재 워크플로우 (`/workflow`)
**용도**: 기안, 검토, 합의, 승인 등 전자결재 프로세스

**노드 타입**:
- 기안 (start) - 문서 작성 시작
- 검토 (review) - 반려 가능, N번 반복
- 합의 (agreement) - 반려 옵션, 병렬 처리 가능
- 승인 (approval) - 문서번호 채번, 내용 잠금
- 시행 (execution) - 외부 발송
- 접수 (receipt) - 접수번호 채번, 내부 승인 재시작
- 반려 (reject) - 기안자에게 반송
- 조건 분기 (condition) - 금액/부서/직급 기준
- 종료 (end)

**주요 기능**:
- 문서번호 자동 채번
- 다단계 결재선 구성
- 반려 및 재상신 처리
- 외부 발송 및 접수 관리

---

### 2. 게시판 워크플로우 (`/workflow/board`)
**용도**: 게시물 작성, 검수, 게시, 보관 등 게시판 생명주기 관리

**노드 타입**:
- 게시물 작성 (start)
- 검수 (moderate) - 부적절한 내용 검토
- 게시 (publish) - 게시판 공개
- 상단 고정 (pin) - 중요 공지 상단 노출
- 보관 (archive) - 기한 만료 시 자동 보관
- 조건 분기 (condition)
- 종료 (end)

**설정 항목** (boardConfig):
- 게시판 ID / 카테고리 ID
- 상단 고정 여부
- 게시 기간 (일)
- 댓글 허용 여부
- 공개 여부

---

### 3. 업무제안 워크플로우 (`/workflow/proposal`)
**용도**: 아이디어 제안, 평가, 투표, 실행, 보상 관리

**노드 타입**:
- 제안서 작성 (start)
- 평가 (evaluate) - 제안서 심사
- 투표 (vote) - 다수결 투표
- 실행 (implement) - 제안 실행 단계
- 보상 (reward) - 우수 제안 포상
- 조건 분기 (condition)
- 종료 (end)

**설정 항목** (proposalConfig):
- 평가자 목록
- 최소 점수 (통과 기준)
- 투표 기간 (일)
- 포상금 (원)
- 실행 기한

---

### 4. 문서관리 워크플로우 (`/workflow/document`)
**용도**: 버전 관리, 체크아웃/체크인, 잠금 등 문서 버전 관리

**노드 타입**:
- 문서 생성 (start) - v1.0 생성
- 버전 생성 (version) - 새 버전 생성
- 체크아웃 (checkout) - 편집 잠금
- 체크인 (checkin) - 변경사항 저장
- 잠금 (lock) - 읽기 전용
- 잠금 해제 (unlock)
- 보관 (archive) - 장기 보존
- 조건 분기 (condition)
- 종료 (end)

**설정 항목** (documentConfig):
- 주 버전 / 부 버전 (1.0, 2.1 등)
- 체크아웃 사용자
- 잠금 사유
- 보존 기간 (년)
- 보관 여부

---

## 🎯 사용 방법

### 1. 워크플로우 타입 선택
1. 홈페이지에서 워크플로우 카테고리 선택
2. 또는 직접 URL 접속:
   - 전자결재: `/workflow`
   - 게시판: `/workflow/board`
   - 업무제안: `/workflow/proposal`
   - 문서관리: `/workflow/document`

### 2. 노드 추가
- 상단 팔레트에서 노드 타입 버튼 클릭
- 캔버스에 노드가 자동 추가됨

### 3. 연결선 추가
- 방법 1: "연결선 추가" 버튼 → 시작/끝 노드 선택
- 방법 2: (드래그 방식은 환경에 따라 작동할 수 있음)

### 4. 노드 설정
- 노드 클릭 → 우측 Drawer에서 설정
- 각 노드 타입별 전용 설정 항목 제공

### 5. 저장
- "저장" 버튼 클릭
- JSON 파일로 다운로드 (category 필드 포함)

---

## 📊 공통 기능

### 연결선 관리
- **추가**: "연결선 추가" 버튼
- **편집**: 연결선 클릭 → Drawer에서 수정
- **삭제**: 
  - 방법 1: 연결선 클릭 → "삭제" 버튼
  - 방법 2: 연결선 선택 → Delete 키

### 노드 관리
- **추가**: 상단 팔레트 버튼
- **편집**: 노드 클릭 → 설정 변경
- **삭제**: 노드 클릭 → "삭제" 버튼 (시작 노드 제외)

### 라벨 및 애니메이션
- 연결선에 라벨 추가 가능 (예: "승인", "반려")
- 애니메이션 활성화 시 흐르는 점선 효과

---

## 🔧 JSON 구조

### 저장 형식
```json
{
  "category": "board",  // approval | board | proposal | document
  "nodes": [
    {
      "id": "1",
      "type": "start",
      "position": { "x": 250, "y": 0 },
      "data": {
        "label": "게시물 작성",
        "description": "새 게시물 작성",
        "boardConfig": {
          "boardId": "notice",
          "isPinned": false,
          "expiryDays": 30,
          "allowComments": true,
          "isPublic": true
        }
      }
    }
  ],
  "edges": [
    {
      "id": "e1-2",
      "source": "1",
      "target": "2",
      "label": "검수 요청",
      "animated": false
    }
  ],
  "timestamp": "2026-01-11T00:00:00.000Z"
}
```

---

## 💡 사용 시나리오

### 전자결재: 지출 결의서
```
기안(100만원) → 조건분기(금액 체크)
  ├─ ≤500만원 → 팀장 검토 → 부서장 승인
  └─ >500만원 → 팀장 검토 → 부서장 검토 → 대표이사 승인
```

### 게시판: 공지사항
```
작성 → 관리자 검수 → 게시 → (30일 후) → 자동 보관
```

### 업무제안: 개선 아이디어
```
제안 → 위원회 평가(60점 이상) → 투표(7일) → 실행(3개월) → 포상(50만원)
```

### 문서관리: 정책 문서
```
생성(v1.0) → 체크아웃 → 수정 → 체크인(v1.1) → 잠금(승인 대기) → 잠금해제 → 체크아웃 → ...
```

---

## 🎨 노드 색상 가이드

- **보라색**: 시작/버전 (start, version)
- **분홍색**: 검토/체크아웃 (review, checkout)
- **청록색**: 합의/체크인 (agreement, checkin)
- **핑크색**: 승인/잠금 (approval, lock)
- **녹색**: 시행/잠금해제 (execution, unlock)
- **주황색**: 접수/조건분기 (receipt, condition)
- **빨강색**: 반려/잠금 (reject, lock)
- **회색**: 종료 (end)
- **하늘색**: 게시/체크인 (publish, checkin)
- **노랑**: 평가/검수 (evaluate, moderate)

---

**작성일**: 2026-01-11  
**버전**: 2.0.0 (Multi-Category System)
