# 워크플로우 순서 관리 가이드

## 📊 워크플로우 순서 정의 방식

### 1. **엣지(Edge) 기반 순서**

워크플로우의 순서는 **노드 간 연결(edges)**로 정의됩니다:

```json
{
  "nodes": [
    { "id": "1", "type": "start", "data": { "label": "기안" } },
    { "id": "2", "type": "review", "data": { "label": "검토" } },
    { "id": "3", "type": "approval", "data": { "label": "승인" } },
    { "id": "4", "type": "end", "data": { "label": "완료" } }
  ],
  "edges": [
    { "id": "e1-2", "source": "1", "target": "2" },  // 1 → 2
    { "id": "e2-3", "source": "2", "target": "3" },  // 2 → 3
    { "id": "e3-4", "source": "3", "target": "4" }   // 3 → 4
  ]
}
```

**흐름:** 기안(1) → 검토(2) → 승인(3) → 완료(4)

---

### 2. **Sequence 필드 (자동 계산)**

`sequence` 필드는 **"순서 계산" 버튼**을 클릭하면 자동으로 생성됩니다:

```json
{
  "nodes": [
    {
      "id": "1",
      "type": "start",
      "data": {
        "label": "기안",
        "sequence": 1     // 1번째
      }
    },
    {
      "id": "2",
      "type": "review",
      "data": {
        "label": "검토",
        "sequence": 2     // 2번째
      }
    }
  ]
}
```

**표시:** 노드 카드 좌측 상단에 순서 번호(①, ②, ③...)가 표시됩니다.

---

## 🎯 순서 관리 기능

### 1. **순서 계산 (Calculate Sequence)**

**버튼:** 헤더의 "순서 계산" 버튼

**기능:**
- 시작 노드(기안)부터 DFS 알고리즘으로 순회
- 각 노드에 `sequence` 필드 자동 추가
- 노드 카드에 순서 번호 표시

**사용 시점:**
- 노드를 추가/삭제/연결 변경 후
- 순서를 명확히 확인하고 싶을 때
- JSON 저장 전 순서 정보 추가

**예시:**
```
기안(1) → 검토(2) → 합의(3) → 승인(4) → 완료(5)
```

---

### 2. **자동 정렬 (Auto Layout)**

**버튼:** 헤더의 "자동 정렬" 버튼

**기능:**
- 순서 계산 + 노드 위치 자동 조정
- 세로 방향 정렬 (위→아래)
- 분기 시 가로 방향 분산 배치

**정렬 규칙:**
- 단일 경로: 직선 세로 배치 (간격 150px)
- 분기: 가로로 분산 배치 (간격 300px)
- 시작점: x=400, y=50

**Before:**
```
┌─────┐         ┌─────┐
│ 기안 │ ────→ │ 검토 │
└─────┘         └─────┘
                     ↘
  ┌─────┐             ┌─────┐
  │ 승인 │ ←──────── │ 합의 │
  └─────┘             └─────┘
```

**After (자동 정렬):**
```
    ┌─────┐
    │ 기안 │ 1
    └─────┘
       ↓
    ┌─────┐
    │ 검토 │ 2
    └─────┘
       ↓
    ┌─────┐
    │ 합의 │ 3
    └─────┘
       ↓
    ┌─────┐
    │ 승인 │ 4
    └─────┘
```

---

### 3. **분기 처리 (Branching)**

**조건 분기 예시:**

```
    ┌─────────┐
    │  기안   │ 1
    └─────────┘
         ↓
    ┌─────────┐
    │  검토   │ 2
    └─────────┘
         ↓
    ┌─────────┐
    │조건 분기│ 3 (금액 기준)
    └─────────┘
       ↙   ↘
  ┌─────┐   ┌─────┐
  │부서장│ 4 │대표 │ 4
  │승인 │   │승인 │
  └─────┘   └─────┘
       ↘   ↙
    ┌─────────┐
    │  완료   │ 5
    └─────────┘
```

**JSON 구조:**
```json
{
  "edges": [
    { "source": "1", "target": "2" },
    { "source": "2", "target": "3" },
    { "source": "3", "target": "4a", "label": "≤ 500만원" },
    { "source": "3", "target": "4b", "label": "> 500만원" },
    { "source": "4a", "target": "5" },
    { "source": "4b", "target": "5" }
  ]
}
```

---

## 🛠️ 순서 설정 실전 가이드

### 시나리오 1: 순차 프로세스

**요구사항:** 기안 → 검토 → 합의 → 승인 → 완료

**설정 방법:**
1. 노드 5개 추가 (start, review, agreement, approval, end)
2. 노드를 순서대로 드래그 연결
3. "순서 계산" 버튼 클릭
4. 각 노드에 1, 2, 3, 4, 5 번호 표시 확인
5. "자동 정렬" 버튼으로 세로 정렬

---

### 시나리오 2: N번 반복 검토

**요구사항:** 기안 → 팀장 검토 → 부서장 검토 → 본부장 검토 → 승인

**설정 방법:**
1. 검토 노드 3개 추가
2. 각 검토 노드 설정:
   - 노드 1: "팀장 검토", 순서(order) 1
   - 노드 2: "부서장 검토", 순서(order) 2  
   - 노드 3: "본부장 검토", 순서(order) 3
3. 순차적으로 연결
4. "순서 계산" 버튼 클릭

**결과:**
```
기안(1) → 팀장 검토(2) → 부서장 검토(3) → 본부장 검토(4) → 승인(5)
```

---

### 시나리오 3: 조건 분기

**요구사항:** 
- 500만원 이하: 부서장 승인
- 500만원 초과: 대표이사 승인

**설정 방법:**
1. 조건 분기 노드 추가
2. 조건 설정:
   - 필드: "amount"
   - 연산자: ">"
   - 값: "5000000"
3. 승인 노드 2개 추가 (부서장, 대표이사)
4. 조건 노드에서 두 승인 노드로 각각 연결
5. 엣지 라벨 설정:
   - True 경로: "> 500만원"
   - False 경로: "≤ 500만원"
6. "순서 계산" 버튼 클릭

**결과:**
```
기안(1) → 검토(2) → 조건 분기(3)
                        ├─ True → 대표이사 승인(4) → 완료(5)
                        └─ False → 부서장 승인(4) → 완료(5)
```

**주의:** 분기 후 두 경로의 sequence가 동일(4)할 수 있습니다. 이는 병렬 처리를 의미합니다.

---

### 시나리오 4: 병렬 합의

**요구사항:** 법무팀, 재무팀 동시 합의

**설정 방법:**
1. 합의 노드 2개 추가
2. 각 노드 설정:
   - "법무팀 합의", 병렬 모드: true
   - "재무팀 합의", 병렬 모드: true
3. 이전 노드에서 두 합의 노드로 각각 연결
4. 두 합의 노드를 다음 노드로 연결

**결과:**
```
기안(1) → 검토(2) ┬─ 법무팀 합의(3) ─┐
                   └─ 재무팀 합의(3) ─┤
                                     └─→ 승인(4)
```

---

## 📊 순서 시각화

### 노드 카드 표시

```
┌────────────────────────┐
│ ① 📝 기안              │
│ 문서 작성 및 제출       │
└────────────────────────┘
         ↓
┌────────────────────────┐
│ ② 📋 부서장 검토       │
│ 부장 | 재무팀          │
│ 🔴 반려 가능           │
└────────────────────────┘
         ↓
┌────────────────────────┐
│ ③ ✅ 대표이사 승인     │
│ 대표이사 | 경영지원실  │
│ 🟢 문서번호 채번       │
│ 🔴 변경 불가           │
└────────────────────────┘
```

- **좌측 상단 원형 뱃지**: 순서 번호 (①, ②, ③...)
- **아이콘**: 노드 타입
- **Tag**: 설정 정보 (반려 가능, 문서번호 채번 등)

---

## 💡 순서 관리 팁

### 1. 노드 추가/삭제 후 항상 순서 재계산
```
노드 변경 → "순서 계산" 버튼 → 저장
```

### 2. 복잡한 워크플로우는 자동 정렬 사용
```
노드 배치 완료 → "자동 정렬" 버튼 → 가독성 향상
```

### 3. JSON 저장 전 순서 확인
```json
{
  "data": {
    "label": "검토",
    "sequence": 2  // ← 이 필드 확인
  }
}
```

### 4. 분기 시 엣지 라벨 활용
```json
{
  "edges": [
    {
      "source": "3",
      "target": "4a",
      "label": "≤ 500만원"  // ← 조건 명시
    }
  ]
}
```

---

## 🔍 순서 검증

### 자동 검증 항목 (예정)

- ✅ 시작 노드(기안) 존재 확인
- ✅ 종료 노드 존재 확인
- ✅ 고아 노드(연결 안 된 노드) 체크
- ✅ 순환 참조(무한 루프) 검증
- ✅ 조건 분기의 모든 경로 연결 확인

---

## 📚 참고 자료

- **DFS (Depth-First Search)**: 그래프 순회 알고리즘
- **DAG (Directed Acyclic Graph)**: 방향성 비순환 그래프
- **React Flow**: 노드 기반 다이어그램 라이브러리

---

**작성일:** 2024-01-11  
**버전:** 1.1.0
